<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.devoligastudio.chat">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logochat"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Chatroom"
            android:parentActivityName=".MainActivity"
            ></activity>
        <activity android:name=".fragmentos.fragmentochatroom" />
        <activity
            android:name=".Conversa"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Cadastrar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".validacao" />
        <activity android:name=".Login" />
        <activity android:name=".Logar_com_email">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>